# v0.1.13.0

Presentation + Dimension severity calibration release.

- Hide Rule provenance from author-facing UI while retaining it internally.
- Remove system jargon from Problem Clusterer author text and deterministic clustered fallback.
- Shorten NOT_RUN verification notice on frontend.
- Calibrate Dimension threshold so local/single non-BLOCKER problems can coexist with a Dimension=`达标`; `有明显问题` means the dimension as a whole is below publish-ready standard.
